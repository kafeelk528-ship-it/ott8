{% extends "base.html" %}
{% block title %}Home — OTT Store{% endblock %}
{% block content %}
<section class="hero">
  <div class="hero-left">
    <h1>Premium OTT Subscriptions — Delivered Fast</h1>
    <p class="lead">Choose from top providers, add to cart, and checkout securely (Stripe test).</p>
    <div class="hero-ctas">
      <a class="btn primary" href="{{ url_for('show_plans') }}">Shop Plans</a>
      <a class="btn ghost" href="{{ url_for('cart') }}">View Cart ({{ session.get('cart_count', 0) }})</a>
    </div>
  </div>
  <div class="hero-right">
    <img src="{{ url_for('static', filename='img/logo-light.png') }}" alt="promo" class="hero-image">
  </div>
</section>

<section class="featured container">
  <h2>Featured Plans</h2>
  <div class="grid">
    {% for p in plans[:4] %}
    <div class="product-card">
      <img class="provider" src="{{ url_for('static', filename='ott/' ~ p['logo']) }}" alt="{{ p['name'] }}">
      <h3>{{ p['name'] }}</h3>
      <div class="price">₹{{ p['price'] }}</div>
      <div class="card-actions">
        <a class="btn outlined" href="{{ url_for('plan_details', plan_id=p['id']) }}">Details</a>
        <a class="btn primary" href="{{ url_for('add_to_cart', plan_id=p['id']) }}">Add to Cart</a>
      </div>
    </div>
    {% endfor %}
  </div>
</section>
{% endblock %}
